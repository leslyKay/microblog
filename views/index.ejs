 
 
 

    <div class="hero-unit" >  
  <h1>欢迎来到 Microblog</h1> 
  <p> Microblog 是一个基于 Node.js、mongodb、mooges、redis、ejs、boostrap 的微博系统。</p> 
  <p> 
    <a class="btn btn-primary btn-large" href="/login" > 登录</a> 
    <a class="btn btn-large" href="/reg"> 立即注册</a> 
  </p> 
</div>  
 
<% posts.forEach(function(post, index) {  
  if (index % 3 == 0) { %> 
    <div class="row"> 
<%} %> 
  <div class="span4"> 
 <h2><a href="/u/<%= post.user %>"><%= post.user %></a> 说</h2> 
    <p><small><%= post.post_time %></small></p> 
    <p><%= post.post %></p> 
  </div> 
<% if (index % 3 == 2) { %> 
    </div><!-- end row --> 
<% } %> 
<%}); %> 
<% if (posts.length % 3 != 0) { %> 
  </div><!-- end row --> 
<%} %>
 

